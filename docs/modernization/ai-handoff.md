# AI Handoff Bridge

This tracked file is a compatibility bridge only.

- Active modernization AI handoff now lives in local-only `.ai/modernization/ai-handoff.md`.
- Use `docs/ai-relay.md` to find the authoritative active lane files.
- Do not treat this tracked file as current AI state.
- Keep `docs/modernization/` for stable human-facing references; keep volatile relay state under `.ai/`.
