# Dependency Ledger Bridge

This tracked file is a compatibility bridge only.

- Active modernization dependency-planning state now lives in local-only `.ai/modernization/dependency-ledger.md`.
- Use `docs/ai-relay.md` to find the active lane, then read the `.ai/` files it points to.
- Stable human-facing dependency and build references remain under `docs/modernization/`.
